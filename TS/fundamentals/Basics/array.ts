const heros: string[] = [];
const superPowers: Array<number> = [];

heros.push('thor')
superPowers.push(5)

type User = {
    name: string;
    isActive: boolean
} 

const allUsers: User[] = []

const MLModels: number[][] = [
    [1,2,3,4]
]

allUsers.push({name: "camillo", isActive: false})